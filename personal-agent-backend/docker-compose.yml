version: "3.8"
services:
  agent:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./chroma_data:/app/chroma_data
      - ./agent.db:/app/agent.db
      - ../persona:/app/persona:ro
    restart: unless-stopped
