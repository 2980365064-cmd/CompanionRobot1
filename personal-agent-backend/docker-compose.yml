version: "3.8"
services:
  agent:
    build: .
    ports:
      - "8001:8001"
    env_file:
      - .env
    volumes:
      - ./chroma_data:/app/chroma_data
      - ./agent.db:/app/agent.db
      - ../persona:/app/persona:ro
    restart: unless-stopped
